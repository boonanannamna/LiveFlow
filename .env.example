# Used only by the Rust/Tauri backend. Never expose this in React or commit the real value.
DATABASE_URL=postgresql://user:password@host/db?sslmode=require
LIVEFLOW_ADMIN_EMAIL=admin@example.com
LIVEFLOW_ADMIN_PASSWORD=change-me
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=mailer@example.com
SMTP_PASSWORD=app-password
SMTP_FROM_EMAIL=mailer@example.com
SMTP_FROM_NAME=TikLIVE
